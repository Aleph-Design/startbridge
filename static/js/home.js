/*
const form = document.querySelector('#form');

form.addEventListener('submit', (e) => {
  // prevent the form to communicate with the server.
  // this will prevent the form to submit????????????
  e.preventDefault();

  const persFld = document.querySelector('#persoon');
  const coopFld = document.querySelector('#cooperatie');
  if ((persFld.value !== "") && (coopFld.value !== "")) {
    console.log("Zoek op een combinatie");
  } else {
    if (persFld.value !== "") {
      console.log("Zoek een persoon")
    }
    if (coopFld.value !== "") {
      console.log("Zoek een coperatie")
    }
  }

  console.log("submit clicked");

  // NOW, we definitly submit the form.
  // form.submit();
})
*/


// colorBtn = document.getElementById('colorButton');
// colorBtn.AddEventListener('click', (e) => {
//   alert("het werkt");
//   // notify("Some message", "warning");
// })

